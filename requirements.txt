# GenEvolve main runtime / evaluation environment.
# Install torch first as shown in README, then install this file with:
#   pip install --no-build-isolation -r requirements.txt
#
# Qwen-Image-Edit rendering is intentionally run in a separate service
# environment; see README for the qwenimage setup.

# Core model stack
torch==2.8.0
torchvision==0.23.0
transformers==4.57.1
accelerate==1.13.0
datasets==4.8.4
peft==0.19.0
qwen-vl-utils==0.0.14
flash_attn==2.8.3

# Serving and rollout
vllm==0.11.0
flashinfer-python==0.4.1
ray==2.54.1

# Agent/runtime and data utilities
numpy==1.26.4
pandas
polars==1.33.1
pyarrow==23.0.1
Pillow>=10.0
requests==2.32.3
openai>=1.30
python-dotenv>=1.0
PyYAML>=6.0
tqdm
json5
aiosqlite
oss2

# Model/tokenizer utilities
protobuf
sentencepiece
tiktoken
safetensors
