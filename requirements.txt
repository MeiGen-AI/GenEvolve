# Core dependencies for the GenEvolve agent runtime (Environment A).
# These are the lightweight client-side requirements; install one inference
# server (vllm or sglang) separately to actually serve the GenEvolve-8B
# checkpoint. See README §Requirements for full details.
openai>=1.30
requests>=2.28
pillow>=10.0

# --------------------------------------------------------------------------
# Optional GPU / inference-server dependencies (NOT installed by default)
# --------------------------------------------------------------------------
# Install one of these in the same environment to host the released
# GenEvolve-8B checkpoint as an OpenAI-compatible chat-completions service:
#
#   vllm>=0.11
#   sglang[all]>=0.5.4
#
# Qwen3-VL fused attention (recommended, matches training-time kernel):
#
#   flash-attn==2.8.3        # pip install flash-attn==2.8.3 --no-build-isolation
#
# --------------------------------------------------------------------------
# Environment B — local Qwen-Image-Edit-2511 renderer (`pip install -e ".[qwen]"`)
# --------------------------------------------------------------------------
# torch>=2.6,<2.7
# diffusers>=0.38
# transformers>=4.55
# accelerate>=1.0
